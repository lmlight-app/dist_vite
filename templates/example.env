# =============================================================================
# DigitalBase Configuration — Ollama Edition
# =============================================================================
# このファイルを ~/.local/db/.env にコピーして編集してください。
#   cp templates/example.env ~/.local/db/.env
#   chmod 600 ~/.local/db/.env
#
# install-docker.sh を使う場合は自動生成されるので通常このファイルは不要です。
# 環境変数は --env-file で渡されてコンテナ内に注入されます。
# =============================================================================

# ── Database (必須) ──────────────────────────────────────────────────────────
# host.docker.internal = Docker から host マシンへの参照 (Mac/Windows)
# Linux の場合は host の実 IP に置き換えてください。
DATABASE_URL=postgresql://digitalbase:digitalbase@host.docker.internal:5432/digitalbase

# ── LLM Backend (必須) ───────────────────────────────────────────────────────
OLLAMA_BASE_URL=http://host.docker.internal:11434

# ── Authentication (必須) ────────────────────────────────────────────────────
# JWT_SECRET は本番では openssl rand -hex 32 で生成した値に必ず差し替える
JWT_SECRET=change-me-to-random-32byte-hex
# OAuth トークン等の暗号化に使用 (空なら JWT_SECRET から派生)
OAUTH_ENCRYPTION_KEY=
# local | ldap | oidc
AUTH_MODE=local

# ── Cloud LLM Provider (任意) ────────────────────────────────────────────────
# 設定した分だけ Bot / Chat で選択肢に追加される
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GOOGLE_API_KEY=

# ── LDAP / Active Directory (任意) ───────────────────────────────────────────
# LDAP_SERVER=ldap://ad.example.local
# LDAP_BIND_DN=CN=svc,DC=example,DC=local
# LDAP_BIND_PASSWORD=
# LDAP_BASE_DN=DC=example,DC=local
# LDAP_USE_TLS=true

# ── License (オプション、デフォルト推奨) ──────────────────────────────────────
# LICENSE_FILE_PATH=/app/data/license.lic

# ── Server (オプション、デフォルト推奨) ───────────────────────────────────────
# API_HOST=0.0.0.0
# API_PORT=8000
